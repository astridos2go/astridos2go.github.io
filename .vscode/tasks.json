{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tailwind in Watch Mode",
            "type": "shell",
            "command": "npx tailwindcss -i ./css/input.css -o ./css/output.css --watch",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}